apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
  namespace: dev_wordpress
spec:
  selector:
    app: wordpress
  ports:
    - protocol: TCP
      port: 80        
      targetPort: 80  
      nodePort: 30080 
  type: NodePort
